{
  "name": "proxyweb-node-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js HTTP proxy backend for proxyWeb",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node main.js",
    "dev": "node --watch main.js",
    "test": "node --test --test-concurrency=1",
    "test:unit": "node --test --test-concurrency=1 tests/unit/*.test.js",
    "test:integration": "node --test --test-concurrency=1 tests/integration/*.test.js",
    "lint": "node --check main.js && node --check app.js && node --check config/defaults.js && node --check config/schema.js && node --check config/loader.js && node --check tests/fixtures/upstream-server.js && node --check tests/helpers/fixture-dns-preload.js && node --check tests/helpers/proxy-process.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "basic-auth": "^2.0.1",
    "chokidar": "^4.0.3",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "ipaddr.js": "^2.2.0",
    "winston": "^3.17.0",
    "zod": "^4.4.3"
  }
}
